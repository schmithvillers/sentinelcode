# SentinelCode

SentinelCode provides runtime security for
autonomous coding agents.

## Installation

Create a virtual environment:

python -m venv .venv

Install dependencies:

pip install -r requirements.txt

Run tests:

pytest